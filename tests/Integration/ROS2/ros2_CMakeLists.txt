cmake_minimum_required(VERSION 3.15)
project(hello_worlds_consumer)

find_package(ament_cmake REQUIRED)
find_package(hello_worlds REQUIRED)

add_executable(consumer_app consumer_test.cpp)
target_link_libraries(consumer_app hello_worlds::hello_worlds_physics)

install(TARGETS consumer_app DESTINATION lib/${PROJECT_NAME})

ament_package()